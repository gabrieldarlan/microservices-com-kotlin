server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/gabrieldarlan/config-server-ms-kotlin2
          search-paths:
            - 'greeting-service'